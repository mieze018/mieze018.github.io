// ⚛️
import React, { memo } from 'react';
// 🧩

const View = memo(() => {
  //🏁GetAPI start
  document.querySelector('body')!.classList.add('page-index');
  return <></>;
});

export default View;
